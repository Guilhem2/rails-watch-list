<h1>Bookmarks</h1>
<ul><% @bookmarks.each do |bookmark| %>
<li><%= link_to bookmark.movie.title, bookmarks_path(bookmark) %><%= " / #{bookmark.list.name} / #{bookmark.comment}  " %><%= link_to "   Delete", bookmarks_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></li>
<% end %></ul>
<h4><%= link_to "return to your movie categories", root_path %></h4>
